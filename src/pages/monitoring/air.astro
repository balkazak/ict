---
import Layout from "@/layouts/Layout.astro";
import { AirCategories } from "@/lib/AirSensors";
import ProductCard from "@/components/ProductCard.astro";
---

<Layout title="Мониторинг воздуха | ICT Central Asia">
  <section class="bg-base-200 py-16">
    <div class="container mx-auto max-w-7xl px-4">
      <div class="text-center mb-12">
        <h1 class="text-4xl font-bold mb-4">Мониторинг Воздуха</h1>
        <p class="text-lg text-gray-600 max-w-3xl mx-auto">
          Комплексные решения для мониторинга качества воздуха, метеорологических параметров и радиации
        </p>
      </div>
      
      {AirCategories.map((category) => (
        <div class="mb-16">
          <div class="text-center mb-8">
            <h2 class="text-3xl font-semibold mb-2 text-primary">{category.name}</h2>
            <div class="w-24 h-1 bg-primary mx-auto rounded"></div>
          </div>
          
          <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
            {category.products.map((product) => (
              <ProductCard {...product} />
            ))}
          </div>
        </div>
      ))}
    </div>
  </section>
</Layout>
