---
import { Image } from "astro:assets";
import techTreeImage from "@/assets/hero/tech-tree.png";
---

<section class="relative min-h-screen overflow-hidden bg-gradient-to-b from-[#0A0A1B] to-[#1A1A3A]">
  <!-- Animated cyber grid -->
  <div class="absolute inset-0">
    <div class="cyber-grid"></div>
  </div>

  <!-- Glowing orbs -->
  <div class="absolute inset-0">
    {Array.from({ length: 15 }).map(() => (
      <div
        class="glow-orb absolute rounded-full"
        style={{
          left: `${Math.random() * 100}%`,
          top: `${Math.random() * 100}%`,
          width: `${Math.random() * 100 + 50}px`,
          height: `${Math.random() * 100 + 50}px`,
          animationDelay: `${Math.random() * 5}s`,
          animationDuration: `${Math.random() * 10 + 10}s`
        }}
      ></div>
    ))}
  </div>

  <!-- Main content -->
  <div class="relative z-10 container mx-auto max-w-6xl px-4 py-20">
    <div class="flex min-h-[80vh] flex-col items-center gap-16 lg:flex-row">
      <div class="space-y-8 lg:w-1/2">
        <!-- Animated tech badge -->
        <div class="cyber-badge" data-aos="zoom-in">
          <div class="cyber-badge-glitch"></div>
          <span class="relative z-10 text-cyan-400">Инновационные технологии будущего</span>
        </div>

        <!-- Glitch effect title -->
        <h1 class="glitch-text text-6xl font-bold text-white md:text-7xl lg:text-8xl" data-text="ICT Central Asia">
          ICT Central Asia
        </h1>

        <!-- Animated description -->
        <p class="cyber-text text-lg text-cyan-200/80">
          Совместное предприятие <span class="text-cyan-400">Proyectae</span> и
          <span class="text-cyan-400">ICT International</span>, создающее будущее через
          передовые технологии и инновации в области устойчивого развития.
        </p>

        <!-- Stats with cyber effect -->
        <div class="cyber-stats grid grid-cols-3 gap-6">
          {[
            { number: "40+", text: "лет опыта" },
            { number: "500+", text: "проектов" },
            { number: "50+", text: "стран" }
          ].map((stat) => (
            <div class="cyber-stat-card group">
              <div class="cyber-stat-number">{stat.number}</div>
              <div class="cyber-stat-text">{stat.text}</div>
            </div>
          ))}
        </div>
      </div>

      <!-- Animated image section -->
      <div class="relative lg:w-1/2">
        <div class="cyber-frame">
          <div class="cyber-scanner"></div>
          <Image
            src={techTreeImage}
            alt="Future Technology Concept"
            class="cyber-image"
            width={600}
            height={600}
          />
          <div class="cyber-corners"></div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .cyber-grid {
    background: 
      linear-gradient(90deg, rgba(0,255,255,0.1) 1px, transparent 1px),
      linear-gradient(rgba(0,255,255,0.1) 1px, transparent 1px);
    background-size: 50px 50px;
    width: 100%;
    height: 100%;
    animation: gridMove 20s linear infinite;
    transform-origin: center;
    transform: perspective(1000px) rotateX(60deg);
  }

  .glow-orb {
    background: radial-gradient(circle at 30% 30%, rgba(0,255,255,0.3), transparent);
    filter: blur(20px);
    animation: floatOrb infinite ease-in-out both;
  }

  .cyber-badge {
    position: relative;
    padding: 0.5rem 1.5rem;
    background: rgba(0,255,255,0.1);
    border: 1px solid rgba(0,255,255,0.2);
    border-radius: 4px;
    overflow: hidden;
  }

  .cyber-badge-glitch {
    position: absolute;
    inset: 0;
    background: rgba(0,255,255,0.2);
    animation: glitch 2s infinite;
    clip-path: polygon(0 0, 100% 0, 100% 100%, 0 100%);
  }

  .glitch-text {
    position: relative;
    text-shadow: 
      0 0 10px rgba(0,255,255,0.5),
      0 0 20px rgba(0,255,255,0.3),
      0 0 30px rgba(0,255,255,0.2);
    animation: textGlitch 3s infinite;
  }

  .cyber-frame {
    position: relative;
    border: 2px solid rgba(0,255,255,0.2);
    border-radius: 8px;
    overflow: hidden;
  }

  .cyber-scanner {
    position: absolute;
    width: 100%;
    height: 2px;
    background: rgba(0,255,255,0.5);
    filter: blur(2px);
    animation: scan 3s ease-in-out infinite;
  }

  .cyber-corners::before,
  .cyber-corners::after {
    content: '';
    position: absolute;
    width: 20px;
    height: 20px;
    border: 2px solid rgba(0,255,255,0.5);
  }

  .cyber-corners::before {
    top: -2px;
    left: -2px;
    border-right: 0;
    border-bottom: 0;
  }

  .cyber-corners::after {
    bottom: -2px;
    right: -2px;
    border-left: 0;
    border-top: 0;
  }

  .cyber-stat-card {
    background: rgba(0,255,255,0.05);
    border: 1px solid rgba(0,255,255,0.1);
    padding: 1rem;
    border-radius: 4px;
    transition: all 0.3s ease;
  }

  .cyber-stat-card:hover {
    background: rgba(0,255,255,0.1);
    transform: translateY(-5px);
    box-shadow: 0 10px 20px rgba(0,255,255,0.1);
  }

  .cyber-stat-number {
    font-size: 2rem;
    font-weight: bold;
    color: #00ffff;
    text-shadow: 0 0 10px rgba(0,255,255,0.5);
  }

  .cyber-stat-text {
    color: rgba(0,255,255,0.7);
    font-size: 0.875rem;
  }

  @keyframes gridMove {
    0% { transform: perspective(1000px) rotateX(60deg) translateY(0); }
    100% { transform: perspective(1000px) rotateX(60deg) translateY(50px); }
  }

  @keyframes floatOrb {
    0%, 100% { transform: translate(0, 0); opacity: 0.3; }
    50% { transform: translate(-20px, -20px); opacity: 0.6; }
  }

  @keyframes glitch {
    0%, 100% { clip-path: polygon(0 0, 100% 0, 100% 100%, 0 100%); }
    20% { clip-path: polygon(0 20%, 100% 20%, 100% 21%, 0 21%); }
    40% { clip-path: polygon(0 52%, 100% 52%, 100% 53%, 0 53%); }
    60% { clip-path: polygon(0 81%, 100% 81%, 100% 82%, 0 82%); }
  }

  @keyframes textGlitch {
    0%, 100% { transform: translate(0); }
    20% { transform: translate(-2px, 2px); }
    40% { transform: translate(2px, -2px); }
    60% { transform: translate(-2px, -2px); }
  }

  @keyframes scan {
    0% { top: 0; }
    50% { top: 100%; }
    100% { top: 0; }
  }
</style>
